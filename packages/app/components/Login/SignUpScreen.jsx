import * as React from "react";
import { Button, TextInput, View, Text, StyleSheet, Alert } from "react-native";
import { Formik, ErrorMessage } from "formik";

export default function SignUpScreen() {
  return <Text>hi from signup</Text>;
}
